/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['img.icons8.com', 'res.cloudinary.com', 'media.graphassets.com']
  }
}

module.exports = nextConfig
